import 'package:flutter/widgets.dart';

class MPWebView extends StatelessWidget {
  final String url;
  MPWebView({required this.url});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

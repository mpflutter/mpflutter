name: Build and deploy

on:
  push:
    tags:
      - "*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js 16.x
      uses: actions/setup-node@v1
      with:
        node-version: 16.x
    - name: Build
      run: |
        cd packages/mp_dom_runtime/
        npm install -g browserify uglify-js typescript
        npm install
        npm run build
        git config --global user.email "ci@mpflutter.com"
        git config --global user.name "GitHub CI bot"
        git clone https://github.com/mpflutter/dist.git mpflutter_dist
        mkdir -p mpflutter_dist/${{ github.ref }}
        cp -rf dist_web mpflutter_dist/${{ github.ref }}/dist_web
        cp -rf dist_swan mpflutter_dist/${{ github.ref }}/dist_swan
        cp -rf dist_weapp mpflutter_dist/${{ github.ref }}/dist_weapp
        cd mpflutter_dist
        git add .
        git diff-index --quiet HEAD || git commit --message "CI Bot commit"
        git push "https://ponycui:${{ secrets.GH_TOKEN }}@github.com/mpflutter/dist.git" --set-upstream "master"
 